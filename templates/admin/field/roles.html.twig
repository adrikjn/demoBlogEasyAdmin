{% block _User_roles %}
{# block _Entity_propriété #}

{# {{ dump() }} #}

{% for role in field.value %}
    {% if role == "ROLE_USER" %}
        Membre
    {% elseif role == "ROLE_ADMIN" %}
        Admin
    {% endif %}
{% endfor %}

{% endblock %}